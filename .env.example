##
# Copy this file to `.env.local` and replace the placeholder values with
# your own. Environment variables prefixed with `NEXT_PUBLIC_` are exposed
# to the browser. Variables without this prefix remain server-only.

# GitHub username used when fetching repositories for the projects section.
NEXT_PUBLIC_GITHUB_USERNAME=fatiharridho

# You can define additional environment variables below as needed.